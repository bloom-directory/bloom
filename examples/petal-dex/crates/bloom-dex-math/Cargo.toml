[package]
name = "bloom-dex-math"
description = "Pure swap math for bloom DEX petals (SwapStrategy trait + constant-product implementation)"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
thiserror = { workspace = true }
uint = "0.9"
