services:
  main:
    # The local validator is intentionally bound to host loopback. This overlay
    # is selected only by the local lane so its verifier reaches that endpoint.
    network_mode: host
