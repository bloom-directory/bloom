[package]
name = "bloom-chain-types"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Wire types for bloom-chain v0 — pure types + SSZ encoding, no I/O"

[dependencies]
serde = { workspace = true, features = ["derive"] }
thiserror.workspace = true
blake3.workspace = true
hex.workspace = true
ethereum_ssz = "0.10"
ethereum_ssz_derive = "1.0.0-beta.2"
zbase32 = "0.1"

[dev-dependencies]
proptest = "1"

[lints.rust]
unsafe_code = "forbid"
