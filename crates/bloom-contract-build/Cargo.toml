[package]
name = "bloom-contract-build"
version = "0.1.0"
edition = "2024"
description = "Build orchestration for bloom-contract: cargo wasm32 builds + manifest emission + verification."
license = "MIT"

[lib]
name = "bloom_contract_build"

[dependencies]
bloom-contract-metadata = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
blake3 = { workspace = true }
hex = { workspace = true }
