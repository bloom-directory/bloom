# Historical VFS/CLI Parity Ledger

Date: 2026-06-28
Inspected Bloom baseline commit: `1ea1e65` (`Add VFS CLI parity ledger and fund confirm path`)

> Historical snapshot: this ledger describes the baseline above and is not a
> current product contract. In particular, its native Polymarket rows were
> superseded by the external package at `/petals/polymarket/`.

This ledger classified user-facing workflows across the `bloom` CLI and VFS.
Statuses:

- `parity`: both surfaces complete the workflow end-to-end with equivalent gates.
- `cli_only`: only the CLI intentionally supports it today.
- `vfs_only`: only the VFS intentionally supports it today.
- `hybrid_required`: current product flow intentionally combines VFS staging/review with CLI or foreground VFS execution.
- `gap`: product direction needs parity, but one side is missing or unsafe.

Benchmark tracks:

- `track_a`: eligible for equal-capability CLI-vs-VFS benchmark.
- `track_b`: eligible only for current-product recommendation benchmark.
- `track_c`: implement before parity benchmarking.
- `exclude`: unsafe, live-only, flaky, or not agent-relevant for parity yet.

| Workflow | CLI command(s) | VFS path(s) | Shared core function | Safety gates | Artifacts | Parity status | Benchmark eligibility | Tests | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| wallet create/import/list/address | `bloom wallet new <name> [--local]`, `bloom wallet import <name> <private-key> [--local]`, `bloom wallet list`, `bloom wallet address <name> [--qr]` | `write /wallets/new`, `read/list /wallets`, `read /wallets/<name>/address`, `read /wallets/<name>/addresses.json` | `Daemon`, `Keystore`, wallet VFS handler | local/passkey creation ceremony, passphrase for local import, no private key in VFS output | keystore files, config default wallet, audit | `parity` for create/list/address; `cli_only` for raw private-key import | `track_a` except import | existing CLI/VFS wallet tests | Importing a raw secret through VFS should remain excluded unless a redaction-safe design is added. |
| wallet policy read/write/sign/review | `bloom wallet policy <name> ...`, `bloom wallet sign-policy <name>` | `read/write /wallets/<name>/policy.toml`, `write /wallets/<name>/policy-session/new`, policy-session review paths | keystore policy loader/signing and VFS policy-session helpers | passkey/local unlock, policy edit review intent, signed policy enforcement | `policy.toml`, policy signatures, review artifacts | `parity` | `track_a` | existing wallet policy/session tests | `bloom vfs write --unlock-wallet <name>` required for signed writes. |
| wallet outbox stage | CLI stages through command-specific workflows and future raw tx commands | `write /wallets/<name>/chains/<chain>/outbox/new` and workflow-specific staging paths | `TxEngine::stage` | wallet policy, address book, chain config, write permit | outbox pending entry, plan, policy checks, simulation when available | `parity` where a CLI command exists; otherwise `vfs_only` | `track_a` for command-backed flows; `track_b` for VFS-native staging | existing outbox tests | Use workflow rows for benchmark selection. |
| wallet outbox inspect plan/policy/simulation | `bloom vfs cat ...` fallback; command-specific CLIs print plans | `read /wallets/<name>/chains/<chain>/outbox/pending/<id>/{plan.md,policy_check.json,simulation.json}` | outbox store and plan renderer | read-only; no unlock | plan, policy, simulation | `vfs_only` as a namespace; CLI fallback is `bloom vfs cat` | `track_b` | existing VFS tests | Treat `bloom vfs cat` as CLI transport to the VFS, not a separate command surface. |
| wallet outbox confirm/broadcast | command-specific CLIs call confirmation, `bloom vfs write .../confirm --unlock-wallet <name>` | `write /wallets/<name>/chains/<chain>/outbox/pending/<id>/confirm` | `TxEngine::confirm` | passkey/local unlock, review hash, policy re-check, broadcast gates | sent outbox entry, tx hash, review approval | `parity` | `track_a` | existing outbox confirm tests | This is the model for value-moving VFS writes. |
| wallet outbox replace/cancel | `bloom wallet cancel <wallet> <chain> <id>`, `bloom wallet replace <wallet> <chain> <id> [--intent <body>]` | `write /wallets/<wallet>/chains/<chain>/outbox/pending/<id>/cancel`, `write /wallets/<wallet>/chains/<chain>/outbox/pending/<id>/replace` | dedicated CLI commands dispatch through the same VFS write paths; `WalletsHandler::write_outbox` calls `TxEngine::cancel` / `TxEngine::replace_with_intent` | passkey/local unlock, policy gate, nonce preservation, fee bump floor, pending-state checks, broadcast gates | replacement/cancel tx hash and outbox artefacts | `parity` | `track_a` under local/mock chains | CLI help smoke plus existing `TxEngine` replacement/cancel tests and VFS control-file tests | `confirm` body `cancel` remains local pending cancellation; the explicit `cancel` file/command is same-nonce network cancellation. |
| requests new/plan/confirm/body/receipt | `bloom request new <request> [--wallet <name>] [--dry-run]` plus `bloom vfs` fallback | `write /requests/new`, `write /requests/new.dry-run`, `read /requests/{latest,pending/<id>}/plan.md`, `write /requests/pending/<id>/confirm`, response and receipt paths | `RequestsHandler`, payment backend | payment policy, paid/free distinction, warning override sentinel, credential redaction | request, challenge, policy check, response body, receipt, audit | `parity` for fallback CLI/VFS; dedicated CLI only creates | `track_a` for VFS transport comparison | existing request handler tests | Dedicated CLI still hands off to VFS for review/confirm. |
| Enso intent new/plan/simulation/confirm/stage/broadcast | no dedicated core CLI; use the installed Petal through `bloom vfs` | `write /petals/enso/intents/<name>/new`, read session plan/policy/simulation, `write /petals/enso/intents/<name>/<session>/confirm`, then outbox confirm | `bloom-petal-enso`, `TxEngine::stage`, `TxEngine::confirm` | `[defi]` policy, route policy re-check at confirm, receiver classification, outbox owner gate | Petal session state, plan, outbox entries, review intents | `vfs_only` / `hybrid_required` | `track_b` | Enso Petal repository checks | Application workflow, not a core CLI-vs-VFS parity candidate. |
| Polymarket onboarding | `bloom polymarket onboard <name> [--target-pusd <amt> --max-spend <amt>] [--confirm-risk]` | `write /polymarket/onboard/<name>/begin`, read status/plan/approvals | onboarding state machine | geoblock fail-closed, passkey/local unlock, relayer/builder key mode, one funding pass fuse | onboarding state, audit, CLOB creds, builder key metadata | `hybrid_required` | `track_b` | existing onboarding VFS tests | VFS can run onboarding when foreground unlock is available; CLI remains clearer for long-running ceremonies. |
| Polymarket buy/order draft | `bloom polymarket order <name> <slug> yes|no <amount> --max-price <p> [--limit-price <p>] [--dry-run]` | `write /polymarket/trade/<name>/new`, read `drafts/<id>/*` | CLI `place`; VFS `create_trade_draft`; both use `bloom_polymarket::trade` and `OrderStore` but not one shared service | policy at draft, geoblock, market snapshot, onboarding/tradeable check | order draft, plan, quote, policy check, review intent | `parity` for drafting; not for posting | `track_a` for draft-only benchmark | existing VFS trade draft tests | Confirm is separate row. |
| Polymarket buy/order confirm/post | `bloom polymarket confirm <name> <draft-id> [--confirm-risk]` | `write /polymarket/trade/<name>/drafts/<id>/confirm --unlock-wallet <name> --data confirm`; mounted handler advertises but refuses execution | `commands::polymarket::confirm` calls the existing `execute` path | stale-draft refusal, geoblock, policy re-check, lock, passkey/local unlock, CLOB post/reconcile, receipts | signed/posted draft, receipt, audit | `parity` for foreground CLI VFS path; mounted handler is discovery/refusal only | `track_a` for `bloom polymarket confirm` vs `bloom vfs write .../confirm --unlock-wallet` under mocks/local dev | parser tests, handler discovery/refusal test, CLI subprocess parity test for durable missing-draft refusal; existing order execution tests cover shared core gates | Implemented after fund-confirm. Mounted VFS cannot sign because the signer must live in the foreground process. |
| Polymarket sell-to-close draft/confirm | `bloom polymarket sell <name> <slug> yes|no <shares> --min-price <p> [--dry-run]`, `bloom polymarket confirm <name> <draft-id>` | `write /polymarket/trade/<name>/new` with `"side":"sell"` for draft; `write /polymarket/trade/<name>/drafts/<id>/confirm --unlock-wallet <name> --data confirm` for confirm | same as buy rows | sell holdings check, risk-reducing geoblock behavior, policy, lock, unlock | draft, plan, quote, receipt after confirm | `parity` for foreground CLI VFS path; mounted handler is discovery/refusal only | `track_a` under mocks/local dev | same confirm parser/discovery/parity tests; sell-specific execution gates remain in shared core | VFS confirm routes through the same `commands::polymarket::confirm` core used by buy drafts. |
| Polymarket cancel order | `bloom polymarket cancel <name> <order-id>` | `write /polymarket/trade/<name>/orders/<order-id>/cancel --data confirm` (direct handler execution) | `commands::polymarket::cancel` (CLI) / `PolymarketHandler::execute_cancel` (VFS) — both call `ClobClient::cancel_order` | CLOB creds, geoblock warning-only, order lock, audit | cancel response, `order_cancelled` audit | `parity` | `track_a` | handler discovery/execution-gate test; cancel execution covered by existing CLI tests | Cancel is risk-reducing and uses L2 CLOB creds (no owner signing), so it runs directly in the mounted handler — no foreground ceremony needed. |
| Polymarket obligations/positions/account reads | `bloom polymarket obligations <name>` plus `bloom vfs cat` | `read /polymarket/positions/<name>/*`, `/polymarket/account/<name>/*` | data/gamma/clob/onboard stores | read-only; account views redact secrets | positions, trades, activity, portfolio, orders, status, funding options | `parity` for reads via CLI VFS; obligations summary is `cli_only` | `track_a` for raw reads; `track_b` for obligations | existing VFS read tests | Dedicated obligations command has richer operational text. |
| Polymarket redeem dry-run/execute | `bloom polymarket redeem <name> <slug> [--dry-run]` | `write /polymarket/redeem/<name>/<slug>/confirm --unlock-wallet <name> --data confirm`; mounted handler advertises but refuses execution | `commands::polymarket::redeem` calls `submit_and_confirm_wallet_batch` | redeemable data check, passkey/local unlock for execute, lock, relayer confirm | plan, audit, confirmed tx | `parity` for foreground CLI VFS path; mounted handler is discovery/refusal only | `track_a` under mocks/local dev | parser tests, handler discovery/refusal test, CLI subprocess parity test | Owner-signed → foreground confirm (signer must be in-process). Print the plan with CLI `--dry-run`; the VFS confirm is execute (dry_run rejected on confirm). |
| Polymarket revoke approvals dry-run/execute | `bloom polymarket revoke-approvals <name> [--dry-run]` | `write /polymarket/revoke-approvals/<name>/request/confirm --unlock-wallet <name> --data confirm`; mounted handler advertises but refuses execution | `commands::polymarket::revoke_approvals` calls `submit_and_confirm_wallet_batch`; post-confirm allowance verification stays inside the fn | passkey/local unlock, lock, post-confirm allowance checks | plan, audit, confirmed tx | `parity` for foreground CLI VFS path; mounted handler is discovery/refusal only | `track_a` under mocks/local dev | parser tests, handler discovery/refusal test, CLI subprocess parity test | Singleton action keyed by the literal `request` segment. Post-confirm on-chain verification lives in the shared fn so CLI and VFS cannot diverge. |
| Polymarket pUSD withdraw dry-run/execute | `bloom polymarket withdraw-pusd <name> <amount|all> [--dry-run]` | `write /polymarket/withdraw/<name>/pusd/confirm --unlock-wallet <name> --data '{"confirm":true,"amount":"<amount|all>"}'`; mounted handler advertises but refuses execution | `commands::polymarket::withdraw_pusd` calls `submit_and_confirm_wallet_batch` | balance check, passkey/local unlock, lock | plan, audit, confirmed tx | `parity` for foreground CLI VFS path; mounted handler is discovery/refusal only | `track_a` under mocks/local dev | parser tests (incl. bare-ack-rejected + missing-amount), handler discovery/refusal test, CLI subprocess parity test | Value-moving; exposed via foreground-confirm only (mounted handler refuses, like fund). The amount travels in the body (the path carries no amount slot); a bare `confirm` is rejected so an agent cannot default to withdrawing everything. |
| Polymarket fund request/stage/execute | `bloom polymarket fund <name> --target-pusd <amt> --max-spend <amt> [--from-token <token>] [--dry-run]`; `bloom polymarket fund <name> --request <id> [--dry-run] [--confirm-risk]` | `write /polymarket/fund/<name>/new`, read `<id>/{plan.md,request.json,status.json}`, `write /polymarket/fund/<name>/<id>/confirm --unlock-wallet <name> --data confirm` | `commands::polymarket::fund_from_request` calls `fund`, `TxEngine::stage`, `TxEngine::confirm` | onboarding deployed/fundable, live balance re-read, live quote/route, fund route policy, EVM policy, passkey/local unlock, outbox review, broadcast, request executed marker | funding request JSON, plan, outbox plan/review, tx hash, updated request status | `parity` for request execution via foreground CLI VFS path; mounted handler advertises but refuses without local signer | `track_a` for `bloom polymarket fund --request` vs `bloom vfs write .../confirm --unlock-wallet` under mocks/local dev | new parser/discovery tests; live execution uses existing funding core gates | Implemented in this change. Mounted VFS cannot sign because the signer must live in the foreground process. |
| Polymarket builder key list/revoke | `bloom polymarket builder-keys list <name>`, `bloom polymarket builder-keys revoke <name> [key]` | `read /polymarket/builder-keys/<name>/keys.json`, `write /polymarket/builder-keys/<name>/revoke` with `confirm` or `{"confirm":true,"key":"..."}` | CLI and VFS both use stored CLOB creds plus `ClobClient::{list_builder_api_keys,revoke_builder_api_key}`; local `BuilderCredentialStore` cleanup matches CLI semantics | CLOB L2 auth only, no owner signing, no fund-moving authority, explicit confirm body for revoke, no secrets exposed | redacted key ids/status, deleted stored `builder_creds.json` when applicable | `parity` | `track_a` under mocked CLOB | VFS body parser tests plus existing CLI helper coverage | Builder secrets/passphrases remain local-only and unreadable; revoke is handler-executable because it cannot move funds. |
| Hyperliquid account/spot/open orders/fills/funding/book/candles/metadata reads | `bloom hyperliquid account`, `spot-state`, `open-orders`, `fills`, `funding`, `book`, `candles`, `metadata`, `test-reads` | `/hyperliquid/<network>/{mids,perp_meta,perp_contexts,predicted_fundings,spot_meta,spot_contexts}.json`, `/users/<addr>/*`, `/books/<coin>.json`, `/candles/<coin>/<interval>.json`, `/recent_trades/<coin>.json`, `/asset_contexts/<coin>.json`, `/funding_history/<coin>.json` | `bloom_hyperliquid::HyperliquidClient` Info API helpers | read-only, network selection, address/coin/interval validation | JSON reads | `parity` | `track_a` under mocked Info endpoint | existing handler/CLI tests; exact matrix documented in gap plan | CLI reads call the same client directly; VFS exposes the same Info API shapes as files. |
| Hyperliquid owner exchange writes | no broad dedicated CLI for generic order/cancel/schedule/update exchange payloads; `send-asset` and externally signed `raw_signed.json` are separate supported paths | `/hyperliquid/<network>/exchange/<wallet>/{order,cancel,schedule_cancel,update_leverage,raw_signed}.json` | generic owner signing is disabled; `raw_signed.json` validates and submits caller-supplied signatures | no Bloom owner signer for generic writes; externally signed payloads still pass action/file kind and policy validation | `last_response.json` for accepted externally signed payloads | `vfs_only` for externally signed payloads | `track_b` only for `raw_signed.json` validation | existing handler write-surface and policy tests | Product intentionally favors bounded agent sessions over broad owner-signed exchange writes. |
| Hyperliquid agent session lifecycle/actions | `bloom hyperliquid session create|status|audit|stop|cancel-all|close-all` | `/hyperliquid/<network>/agent_sessions/<wallet>/new.json`, `/agent_sessions/<wallet>/<id>/{status.json,session.json,audit.jsonl,last_response.json,order.json,cancel.json,schedule_cancel.json,stop,cancel_all,close_all}` | CLI dispatches through plain VFS IPC; create stages a sealed `approveAgent` action and signs through PetalHost after grant; session actions use ephemeral API wallet signing | Sealed Approval grant for create, policy caps, TTL/loss/leverage/asset bounds, session key isolation, cleanup paths | `approval_challenge.json`, session metadata/audit/last response | `parity` for daemon/foreground VFS path | `track_a` under mocked daemon/exchange endpoints | existing session handler/CLI tests | Product intentionally favors scoped agent sessions over per-order owner signing. |
| Hyperliquid live post-only cancel smoke | `bloom hyperliquid test-post-only-cancel ... --danger-accept-live-orders` | no equivalent first-class VFS path | direct CLI smoke helper is deprecated for owner-signing architecture work | explicit danger flag, max notional cap, post-only price resolver, cleanup cancel if order rests | JSON order/cancel result | `cli_only` | `exclude` from parity benchmark | existing danger-flag/unit tests | This is an operator smoke test, not a user workflow to mirror in VFS. |
| Hyperliquid transfers/USD send | `bloom hyperliquid send-asset <wallet> <destination> <amount> [--network <net>]` | `write /hyperliquid/<network>/exchange/<wallet>/send_asset.json` | dedicated CLI dispatches through plain VFS IPC; `HyperliquidHandler::submit_usd_send` stages a sealed action, signs through PetalHost after grant, and persists `last_response.json` | configured `[hyperliquid]` policy with `transfer_cap_usd`, Sealed Approval grant, exact micro-USDC cap parsing, network selection | `approval_challenge.json`, Hyperliquid exchange response in `last_response.json` | `parity` | `track_a` under mocked exchange endpoint | existing handler write-surface tests plus CLI compile/help coverage | This is not session-agent auth; Hyperliquid requires owner authority, so Bloom obtains it through Sealed Approval host signing rather than wallet unlock. |
| petal install/list/name/uninstall | `bloom petals install`, `bloom petals ls`, `bloom petals name`, `bloom petals uninstall` | VFS petal discovery/read endpoint namespace | petal store / manifest code | capability grants, local file input, no wallet unlock | installed wasm, names, manifests | `cli_only` for install/name/uninstall; VFS read/discovery only | `track_b` | existing petal tests | Mutating local plugin management is CLI-oriented. |
| petal run | `bloom petals run <name-or-hash> [--input <file>] [--cap ...]` | petal endpoint paths under VFS; chain petal view/call surfaces | petal runtime / chain state | capability mask, chain state validation | stdout/result | `hybrid_required` | `track_b` | existing petal tests | Do not implement broad execution parity until product direction is settled. |
| chain init/run-validator/submit/query | `bloom chain init`, `run-validator`, `submit`, `submit-ptb`, `query ...` | chain state read paths in VFS; no VFS node admin | chain node/RPC code | xDSA key handling, node config, UDS RPC | genesis/config, tx receipts, query JSON | `cli_only` for admin/submit; VFS read parity for chain reads | `track_b` reads, `exclude` admin | existing chain tests | Validator admin and raw submit remain intentionally CLI. |
| pipe expression lowering/execution | `bloom pipe <expr> --signer <hex> --gas-payer <hex>`; `bloom chain pipe ...` | `tx/new`, `tx/<id>/{cmd,signer,gas-payer,status,commit,abort}` stage equivalent PTB commands; petal VFS advertises `.pipe` as a CLI shim | `bloom_ptb_builder::PtbSession`; `bloom pipe::lower_and_build`; `TxHandler::commit_ndjson`; daemon `PtbSubmitter` | signer/gas validation, chain execution gates, daemon gas selection when VFS signer is set, unchanged PTB signing/submission | PTB, NDJSON receipt, optional on-chain receipt line | `parity` for staged PTB execution; expression parser is `cli_only` | `track_a` for lowered command/session golden tests; expression-language frontend out of benchmark | existing pipe/tx handler tests | No new VFS expression parser is required: CLI lowering and VFS `tx` sessions already share the canonical `PtbSession` execution substrate. |
